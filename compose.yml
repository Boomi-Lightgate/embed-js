version: '1'

services:
  app:
    container_name: boomi-dropin-ui
    image: node:20-alpine
    volumes:
      - ./:/app
    working_dir: /app
    command: sh -c "npm install && npm run dev"
    ports:
      - '5173:5173'