
export type FormMultiSelectConfig = {
  /**
   * the style for a multiselect
   */
  label?: string;
  labelClass?: string;
  loadingLabel?: string;
  loadingClass?: string;
  table?: TableConfig;
};